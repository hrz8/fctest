{
  "name": "fcprexx",
  "scripts": {
    "build": "yarn build:url-to-image",
    "build:url-to-image": "cd packages/url-to-image && yarn build && cd ..",
    "publish": "yarn build && lerna publish",
    "publish:canary": "yarn build && lerna publish --canary"
  },
  "devDependencies": {
    "lerna": "^5.1.6"
  },
  "version": "1.0.0"
}
